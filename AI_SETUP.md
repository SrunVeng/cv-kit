# Smart Resume Text Suggestions

The app uses a lightweight built-in text generator for resume summaries and highlights.

- No API key is required.
- No backend is required.
- No model download is required.
- Works on mobile and desktop.

The generator uses the user's existing CV fields, selected tone, selected length, and optional notes to create a paste-ready draft. It does not call OpenAI or any external AI service.
